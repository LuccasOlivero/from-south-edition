import styled, { css } from "styled-components";

export const Main = styled.main`
  align-items: center;
  height: 100vh;
  padding-top: 6rem;
  display: flex;
  justify-content: space-between;
  position: relative;
`;
